services:
  api:
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    volumes:
      - ./frontend:/app
      - /app/node_modules

  worker:
    volumes:
      - ./backend:/app

  scheduler:
    volumes:
      - ./backend:/app
