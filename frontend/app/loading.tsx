import { LoadingState } from "@/components/ui/data-state";

export default function GlobalLoading() {
  return <LoadingState title="Loading interface" copy="Preparing modules, session state and tenant context." />;
}
